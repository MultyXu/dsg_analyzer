from setuptools import find_packages, setup

setup(
    name="dsg_analyzer",
    version="0.0.1",
    url="",
    author="",
    author_email="",
    description="DSG Analyzer.",
    packages=find_packages(),
    install_requires=[
        "spark_dsg",
    ],
)
